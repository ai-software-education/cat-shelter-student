version: '3.8'
services:
  db:
    image: postgres:15
    container_name: shelter_db
    restart: always
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: shelter
    ports:
      - "5433:5432"